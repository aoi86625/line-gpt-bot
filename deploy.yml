name: Deploy to Render

on:
  push:
    branches:
      - main  # ← mainブランチに push されたときに実行される

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Render
        run: |
          curl -X POST https://api.render.com/deploy/srv-d01qtkp5pdvs739hq5b0?key=xmJ_UubeQ44
