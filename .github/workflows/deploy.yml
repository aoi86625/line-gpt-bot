name: Deploy to Render

on:
  push:
    branches:
      - main  # ← mainブランチに push されたときに実行される

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Render
        run: |
          curl -X POST https://api.render.com/deploy/srv-xxxxxxxxxxxx?key=xxxxxxxxxxx
